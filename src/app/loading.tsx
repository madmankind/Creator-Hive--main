import { LogoLoader } from "@/components/ui/LogoLoader";

// Next.js route-level loading.tsx — shown while any page in this segment suspends
export default function Loading() {
  return <LogoLoader />;
}
